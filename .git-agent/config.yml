scopes:
    - name: src
      description: application logic, services, models, and CLI commands in Sources/; does not cover testing
    - name: fm
      description: Homebrew formula definition in Formula/; does not cover repository installation logic
